@model IEnumerable<WebApplication2.Models.Control_value>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Control_Value_Description)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.Enclosure_Maintenance_Attribute)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Control_Value_Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Enclosure_Maintenance_Attribute.Attribute_Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Control_ValueID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Control_ValueID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Control_ValueID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
