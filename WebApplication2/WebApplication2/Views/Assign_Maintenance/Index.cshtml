@model IEnumerable<WebApplication2.Assign_Maintenance>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>
<h2>Test Test</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Enclosure)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Enclosure_Maintenance_Type)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Enclosure.EnclosureID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Enclosure_Maintenance_Type.Enclosure_Maintenance_TypeID)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
